apiVersion: v1
kind: Service
metadata:
  name: workhub-service
  namespace: workhub
spec:
  selector:
    app: workhub-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP